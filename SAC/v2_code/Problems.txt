1. Alpha collapse (critical): Hunter α dropped from 1.0 → 0.004 in the first 200k steps post-unfreeze. With TARGET_ENTROPY = -2.0, auto-tuning drives α to near-zero — SAC degenerates to a deterministic policy that immediately locks into a local optimum (orbiting the prey) with zero ability to escape it.
2. Frozen replay buffer poisoning: The hunter was frozen for 2M steps collecting random-action transitions, then trained on that corrupted buffer. Returns briefly spiked to ~108 at step 2.6M then catastrophically regressed as the prey co-evolved against the hunter's now-deterministic orbit behavior.
3. Missing prey velocity + no obstacle bypass direction: The hunter only sees where the prey is, not where it's going — so it forever chases the tail. Additionally, when LOS is blocked, the heading reward still points through walls, creating jitter at obstacle faces.