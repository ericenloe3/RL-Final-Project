The data confirms it perfectly — episode 1 shows a 376-step jitter streak where both agents oscillate between positions (114,453)↔(117,456) and (138,435)↔(136,432) at exactly 30px distance (just above capture_dist=20). Both moving at full speed in opposing directions, distance never changes. Both agents are getting positive heading reward from this standoff, so neither has any incentive to break it.
Two targeted fixes will break this:Strategized reward placement to mitigate agent jittering behaviorStrategized reward placement to mitigate agent jittering behaviorNow add the proximity rewards in the step function. Let me find the right spot:Diagnosed jitter equilibrium, proposed strengthening proximity penaltiesDiagnosed jitter equilibrium, proposed strengthening proximity penaltiesThe proximity penalty is too weak — the prey still gets +0.083/step at the jitter distance. Let me strengthen it:Calibrated reward incentives to discourage jittering behaviorCalibrated reward incentives to discourage jittering behaviorThe numbers now create a clear anti-jitter gradient:
DistanceHunter rewardPrey rewardPrey at range200px (normal chase)+0.090+0.120—60px (zone edge)+0.090+0.120—30px (jitter distance)+0.165+0.045vs +0.120 at range20px (capture distance)+0.190+0.020vs +0.120 at range
The prey now sees that maintaining the standoff at 30px gives +0.045/step, but breaking away to >60px gives +0.120/step — a 2.7× improvement. The rational policy is to break the standoff and flee to open space where the proximity penalty doesn't apply.Verified file modifications and confirmed prior training outputs remained intactVerified file modifications and confirmed prior training outputs remained intactOnly env.py changed — the train/evaluate files in the outputs directory are still current. The fix adds two reward terms that activate only within 60px:
The hunter proximity bonus (+0.10 × proximity_factor) makes the hunter's reward at 30px jump from +0.09 to +0.165. This incentivizes the final lunge — maintaining distance at close range is now suboptimal compared to closing for the capture. The hunter's deterministic policy should shift from "move toward prey at constant heading" to "accelerate toward prey when close."
The prey proximity penalty (-0.10 × proximity_factor) makes the prey's reward at 30px drop from +0.12 to +0.045. Since the prey gets +0.12/step at distances beyond 60px (no proximity penalty), there's now a 2.7× reward difference between "stay in the standoff" and "break away to open space." The prey's value function should learn V(30px standoff) < V(100px with room to maneuver), causing it to prefer breaking the deadlock over maintaining it.
The proximity_factor scales linearly from 0.0 at 60px to 1.0 at 20px (capture distance), so there's no discontinuity and the effect ramps smoothly as agents close in.